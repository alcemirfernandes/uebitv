<!DOCTYPE html>
<html>
<head>
  <title>TomTV</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
	<body class="<%= action_name == 'player' ? 'player' : '' %>">
		<div id="loading" style="display:none;">
		</div>	
		<div> 
			<div class="left">
				<%= image_tag "uebitv.png", :class => 'logo' %> 
			</div>	
			<div class="rigth">
				<i class="icon-facetime-video"></i>
				<%= link_to "Abrir TV", player_path, :target => "blank" %> 
			</div>
		</div>
		<%= yield %>

	</body>
</html>
